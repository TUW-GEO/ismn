name: ismn
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib
  - cartopy
  - scipy
  - pip
  - pip:
     - pygeogrids>=0.3.2
     - configparser
     - tqdm
     - more_itertools
     - pytest
     - pytest-cov